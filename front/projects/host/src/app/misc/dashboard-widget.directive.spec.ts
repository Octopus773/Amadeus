import { DashboardWidgetDirective } from './dashboard-widget.directive';

describe('DashboardWidgetDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardWidgetDirective();
    expect(directive).toBeTruthy();
  });
});
