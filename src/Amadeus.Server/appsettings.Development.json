{
	"ConnectionStrings": {
		"AmadeusDB": "Data Source=<SQL-server-name>.database.windows.net;Initial Catalog=ContosoPets;Connect Timeout=30;Encrypt=True;TrustServerCertificate=False;ApplicationIntent=ReadWrite;MultiSubnetFailover=False"
	},

	"database": {
		"enabled": "postgres",
		"configurations": {
			"sqlite": {
				"data Source": "kyoo.db",
				"cache": "Shared"
			},
			"postgres": {
				"server": "127.0.0.1",
				"port": "5432",
				"database": "bernard",
				"user ID": "ninja",
				"password": "oui",
				"pooling": "true",
				"maxPoolSize": "95",
				"timeout": "30"
			}
		}
	},

	"Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
